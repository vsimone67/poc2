<div class="breadcrumb-background">
    @foreach (var item in _items)
    {
        <RadzenLink class="text-decoration-none align-items-center"
                    Path="@item.Uri"
                    Text="@item.Name"
                    Icon="@item.Icon" />
    }
</div>